<?php

/**
 * La fonction the_contact_class() affiche quelques class
 * dans la balise <article> des contacts
 */

function the_contact_class($row)
{
  if (isset($row)) {
    // Retourne le résultat au template
    echo "string";
  }
}
